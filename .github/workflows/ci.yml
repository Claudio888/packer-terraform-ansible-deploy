name: 'CI'

on: [workflow_dispatch]

jobs:
  packer:
    name: 'Packer'
    runs-on: ubuntu-latest
    container:
      image: hashicorp/packer:latest

    # Use the Bash shell regardless whether the GitHub Actions runner is ubuntu-latest, macos-latest, or windows-latest
    defaults:
      run:
        shell: bash

    steps:
    # Checkout the repository to the GitHub Actions runner
    - name: Checkout
      uses: actions/checkout@v2

    - name: Packer Version
      run: packer --version

    - name: Check path
      run: pwd
    
    - name: Packer Validation
      run: packer validate -syntax-only ./packer/image.json

    - name: Credentials test
      run: echo $ECR_AWS_ACCESS_KEY && echo $ECR_AWS_SECRET_KEY
      env:
        ECR_AWS_ACCESS_KEY: ${{ secrets.ECR_AWS_ACCESS_KEY}}
        ECR_AWS_SECRET_KEY: ${{ secrets.ECR_AWS_SECRET_KEY}}

    - name: Packer execute jobs
      run: packer build ./packer/image.json
      env:
        ECR_AWS_ACCESS_KEY: AKIA5SUL4ZE4FEVBWQ6J
        ECR_AWS_SECRET_KEY: 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
